import 'package:flutter/material.dart';

class RecordsFace extends StatelessWidget {
  const RecordsFace({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: []);
  }
}
